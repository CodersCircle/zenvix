<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro">

    <!-- Premium Developer Light Palette (Hardcoded for Stability) -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="#F8FAFC" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="CardBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="AccentBrush" Color="#6366F1" />
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#0F172A" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#64748B" />
    <SolidColorBrush x:Key="BorderBrush" Color="#E2E8F0" />

    <SolidColorBrush x:Key="SuccessBrush" Color="#22C55E" />
    <SolidColorBrush x:Key="WarningBrush" Color="#F59E0B" />
    <SolidColorBrush x:Key="ErrorBrush" Color="#EF4444" />
    <SolidColorBrush x:Key="OrangeBrush" Color="#F97316" />

    <!-- Typography -->
    <Style x:Key="Hostix.Title" TargetType="TextBlock">
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    </Style>

    <Style x:Key="Hostix.Subtitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
    </Style>

    <!-- Buttons (Premium Light Style) -->
    <Style x:Key="Hostix.Button" TargetType="Button"
        BasedOn="{StaticResource MahApps.Styles.Button.Square}">
        <Setter Property="Height" Value="40" />
        <Setter Property="Padding" Value="16,0" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        <Setter Property="Background" Value="#d6d6d6ff" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="10" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#6FCF97" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Hostix.Button.Primary" TargetType="Button"
        BasedOn="{StaticResource Hostix.Button}">
        <Setter Property="Background" Value="{StaticResource AccentBrush}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderThickness" Value="0" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#4F46E5" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Hostix.Button.Blue" TargetType="Button" BasedOn="{StaticResource Hostix.Button.Primary}">
        <Setter Property="Background" Value="#6366F1" />
        <Setter Property="mah:ControlsHelper.CornerRadius" Value="12" />
    </Style>

    <Style x:Key="Hostix.Button.Orange" TargetType="Button" BasedOn="{StaticResource Hostix.Button}">
        <Setter Property="Background" Value="{StaticResource OrangeBrush}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="mah:ControlsHelper.CornerRadius" Value="12" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#EA580C" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Hostix.Button.Danger" TargetType="Button" BasedOn="{StaticResource Hostix.Button}">
        <Setter Property="Background" Value="{StaticResource ErrorBrush}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style x:Key="Hostix.Button.Ghost" TargetType="Button" BasedOn="{StaticResource Hostix.Button}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                <Setter Property="Background" Value="#636363ff" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Sidebar Navigation (Light) -->
    <Style x:Key="Hostix.NavButton" TargetType="RadioButton">
        <Setter Property="Height" Value="42" />
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="border" Background="Transparent" CornerRadius="8" Padding="15,0">
                        <ContentPresenter VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#686868ff" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#686868ff" />
                            <Setter Property="TextElement.Foreground"
                                Value="{StaticResource AccentBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <!-- Card (Light with subtle shadow) -->
    <Style x:Key="Hostix.Card" TargetType="Border">
        <Setter Property="Background" Value="#e7e7e7ff" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Padding" Value="24" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.05" Color="Black" />
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ToggleSwitch (Mockup Style) - Stable and Interactive Template -->
    <Style x:Key="Hostix.ToggleSwitch.Compact" TargetType="mah:ToggleSwitch">
        <Setter Property="FontSize" Value="10" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="mah:HeaderedControlHelper.HeaderFontSize" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="mah:ToggleSwitch">
                    <Grid x:Name="RootGrid" Background="Transparent" SnapsToDevicePixels="True">
                        <!-- Interaction Layer: Captures clicks and handles state -->
                        <ToggleButton x:Name="PART_Interaction" 
                                      IsChecked="{Binding IsOn, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                      Background="Transparent" 
                                      BorderThickness="0" 
                                      BorderBrush="Transparent"
                                      Padding="0" 
                                      FocusVisualStyle="{x:Null}"
                                      mah:ControlsHelper.FocusBorderBrush="Transparent"
                                      mah:ControlsHelper.MouseOverBorderBrush="Transparent"
                                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                      Cursor="Hand" />

                        <!-- Visual Layer: Does NOT block clicks -->
                        <StackPanel Orientation="Horizontal" IsHitTestVisible="False">
                            <Border x:Name="Track" Width="36" Height="20" Background="#CBD5E1" CornerRadius="10" VerticalAlignment="Center">
                                <Canvas>
                                    <Ellipse x:Name="Thumb" Width="14" Height="14" Fill="White" Canvas.Left="3" Canvas.Top="3">
                                        <Ellipse.RenderTransform>
                                            <TranslateTransform x:Name="ThumbTranslate" X="0" />
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                </Canvas>
                            </Border>
                            <Grid Margin="8,0,0,0" VerticalAlignment="Center">
                                <ContentPresenter x:Name="OffContentPresenter" Content="{TemplateBinding OffContent}" Visibility="Visible" 
                                                  TextBlock.Foreground="{DynamicResource TextSecondaryBrush}" />
                                <ContentPresenter x:Name="OnContentPresenter" Content="{TemplateBinding OnContent}" Visibility="Collapsed" 
                                                  TextBlock.Foreground="{StaticResource AccentBrush}" />
                            </Grid>
                        </StackPanel>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsOn" Value="True">
                            <Setter TargetName="Track" Property="Background" Value="{StaticResource AccentBrush}" />
                            <Setter TargetName="OffContentPresenter" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="OnContentPresenter" Property="Visibility" Value="Visible" />
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ThumbTranslate" Storyboard.TargetProperty="X" To="16" Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ThumbTranslate" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
